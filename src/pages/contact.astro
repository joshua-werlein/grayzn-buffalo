---
import Base from '../layouts/Base.astro';
const env = (Astro.locals as any).runtime?.env ?? {};
const sitekey = env.TURNSTILE_SITEKEY;
---
<Base
  title="Contact — Grayz'n Buffalo Bar & Grill"
  description="Contact Grayz'n Buffalo Bar & Grill in Mondovi, WI for questions, events, menu information, or general inquiries."
>
<Fragment slot="home-footer">
<section class="contact-page">
  <div class="section-head interior-heading">
    <span class="kicker">To get in touch</span>
    <h1>Contact Us</h1>
    <span class="vintage-rule" aria-hidden="true"></span>
    <p>Questions, events, or just want to say hi — drop us a line or give us a call.</p>
  </div>
  <div class="contact-grid">
    <form id="contactForm" class="card">
      <label>Name <input name="name" required autocomplete="name" /></label>
      <label>Email <input name="email" type="email" required autocomplete="email" /></label>
      <label>Message <textarea name="message" rows="5" required></textarea></label>
      <div class="cf-turnstile" data-sitekey={sitekey}></div>
      <button class="btn btn-primary" type="submit">Send Message</button>
      <p id="formStatus" role="status"></p>
    </form>
    <div class="card info">
      <h2>Grayz'n Buffalo Bar &amp; Grill</h2>
      <p><a href="https://www.google.com/maps/search/?api=1&query=102+S+Eau+Claire+St,+Mondovi,+WI+54755" target="_blank" rel="noopener">102 S Eau Claire St, Mondovi, WI</a></p>
      <p><a href="tel:+17159264431">(715) 926-4431</a></p>
      <p>Open daily · 10 AM – 2 AM</p>
      <iframe title="Map to Grayz'n Buffalo"
        src="https://www.google.com/maps?q=102+S+Eau+Claire+St,+Mondovi,+WI+54755&output=embed"
        loading="lazy" referrerpolicy="no-referrer-when-downgrade"
        style="border:0;width:100%;height:260px;border-radius:10px;margin-top:14px"></iframe>
    </div>
  </div>
</section>
</Fragment>
</Base>

<style>
  .contact-page {
    position: relative;
    z-index: 1;
    padding: clamp(34px, 6vw, 72px) clamp(18px, 5vw, 64px) clamp(42px, 7vw, 88px);
    border-top: 0;
    background: transparent;
    color: var(--text);
  }

  /* The shared footer includes this sign on interior pages; Contact has its own heading. */
  :global(body:has(.contact-page) .footer-contact) { display: none !important; }

  .contact-page .section-head {
    width: min(100%, 760px);
    margin: 0 auto clamp(24px, 4vw, 38px);
  }

  .contact-page .section-head .kicker { color: var(--denim); }
  .contact-page .section-head .vintage-rule {
    width: min(15rem, 70%);
    background: linear-gradient(90deg, transparent, var(--brick), transparent);
  }

  .contact-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(18px, 3vw, 34px);
    width: min(100%, 1100px);
    margin: 0 auto;
    align-items: start;
  }

  .card {
    min-width: 0;
    padding: clamp(20px, 3vw, 30px);
    border: 1px solid var(--rule);
    border-top: 5px double var(--brick);
    border-radius: var(--panel-radius);
    background: rgba(255, 248, 226, .64);
    box-shadow: var(--panel-shadow);
  }

  .card.info {
    border-top-color: var(--denim);
    background: rgba(231, 210, 168, .72);
    color: var(--ink);
    box-shadow: var(--panel-shadow-dark);
  }

  .card.info h2 {
    margin-bottom: 10px;
    color: var(--ink);
    font-size: 1.17em;
  }

  .card.info p {
    margin: 8px 0;
    padding-bottom: 8px;
    border-bottom: 1px solid var(--rule);
    color: var(--ink);
  }

  .card.info a {
    color: var(--ink);
    text-decoration-color: var(--brick);
    text-underline-offset: 3px;
  }

  form label {
    display: block;
    margin-bottom: 14px;
    padding-bottom: 7px;
    border-bottom: 1px dotted rgba(43, 27, 20, .38);
    color: var(--text);
    font-family: var(--label);
    font-size: .85rem;
    font-weight: 600;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  form input,
  form textarea {
    display: block;
    width: 100%;
    min-height: 48px;
    margin-top: 5px;
    padding: 11px 12px;
    border: 1px solid rgba(43, 27, 20, .55);
    border-radius: var(--control-radius);
    background: rgba(255, 248, 226, .72);
    box-shadow: inset 0 1px 2px rgba(43, 27, 20, .08);
    color: var(--text);
    font-family: var(--body);
    font-size: 1rem;
  }

  form textarea { min-height: 136px; resize: vertical; }
  form input:focus,
  form textarea:focus { outline: none; border-color: var(--brick); box-shadow: 0 0 0 3px rgba(146, 44, 41, .18); }
  .cf-turnstile { min-height: 65px; margin-bottom: 16px; }
  #formStatus { margin-top: 12px; font-weight: 600; }

  .card.info iframe {
    display: block;
    width: 100%;
    height: clamp(220px, 24vw, 280px) !important;
    margin-top: 14px !important;
    border: 2px solid var(--ochre) !important;
    border-radius: var(--control-radius) !important;
    box-shadow: 2px 2px 0 rgba(43, 27, 20, .18);
  }

  @media (min-width: 1100px) {
    .contact-page {
      /* The route-scoped header rule stays directly beneath navigation while
         this normal-flow inset gives the Contact block breathing room. */
      padding-top: clamp(20px, 2.6vw, 40px);
      padding-bottom: clamp(20px, 2vw, 30px);
    }

    .contact-page .section-head { margin-bottom: clamp(18px, 2vw, 28px); }
    .contact-page .section-head :is(h1, h2) {
      margin: 4px 0 2px;
      font-size: clamp(1.8rem, 3vw, 2.4rem);
    }
    .contact-page .section-head .vintage-rule { margin-top: 6px; }
    .contact-page .section-head p { font-size: 1rem; line-height: 1.35; }

    /* Keep the desktop card pair fully in the image's open sky through a
       compact, normal-flow form rhythm; no offsets or image manipulation. */
    .contact-grid { gap: clamp(18px, 2vw, 28px); }
    .card { padding: 16px; }
    #contactForm {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px 14px;
      align-items: start;
    }
    #contactForm label {
      margin: 0;
      padding-bottom: 4px;
      font-size: .78rem;
    }
    #contactForm label:nth-of-type(3),
    #contactForm .cf-turnstile,
    #contactForm .btn,
    #formStatus { grid-column: 1 / -1; }
    #contactForm .cf-turnstile { min-height: 65px; margin: 0; }
    #contactForm .btn { justify-self: start; }
    #formStatus { margin: 0; }
    form input,
    form textarea { min-height: 44px; margin-top: 4px; padding: 8px 10px; }
    form textarea { min-height: 88px; }
    .card.info h2 { margin-bottom: 8px; }
    .card.info p { margin: 5px 0; padding-bottom: 5px; }
    .card.info iframe {
      height: clamp(170px, 13vw, 190px) !important;
      margin-top: 8px !important;
    }
  }

  @media (max-width: 1099px) {
    /* Tablet and phone are a deliberate one-column reading order. The small
       ending inset is the only exposed parchment after the final card. */
    .contact-page {
      padding-top: clamp(52px, 8vw, 84px);
      padding-bottom: clamp(12px, 1.75vw, 18px);
      background-color: var(--page-parchment);
      background-image: url('/parchment-texture.webp');
      background-position: center;
      background-size: 512px 512px;
    }
    .contact-grid {
      grid-template-columns: 1fr;
      width: min(100%, 760px);
      gap: clamp(16px, 2.5vw, 24px);
    }

  }

  @media (max-width: 520px) {
    .contact-page { padding-inline: 14px; }
    .card { padding: 20px 16px; }
  }
</style>

<script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
<script>
  const form = document.getElementById('contactForm') as HTMLFormElement;
  const status = document.getElementById('formStatus')!;
  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    status.textContent = 'Sending…';
    const data = Object.fromEntries(new FormData(form).entries());
    try {
      const res = await fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });
      const out = await res.json();
      status.textContent = out.ok ? 'Message sent — we\u2019ll get back to you!' : (out.error || 'Something went wrong. Give us a call instead.');
      if (out.ok) form.reset();
    } catch {
      status.textContent = 'Something went wrong. Give us a call instead.';
    }
  });
</script>
