---
import Base from '../layouts/Base.astro';
import { getMenu, getSpecials, getWelcomePhotos, thumbKey, type Item, type WelcomePhoto } from '../lib/db';

const env = (Astro.locals as any).runtime?.env ?? {};
const { categories, items } = await getMenu(env);
const specials = await getSpecials(env);
const weekdayByShortName: Record<string, number> = {
  Sun: 0,
  Mon: 1,
  Tue: 2,
  Wed: 3,
  Thu: 4,
  Fri: 5,
  Sat: 6,
};
// Cloudflare isolates use UTC by default; specials follow Mondovi's calendar day.
const businessWeekday = weekdayByShortName[
  new Intl.DateTimeFormat('en-US', { timeZone: 'America/Chicago', weekday: 'short' }).format(new Date())
] ?? new Date().getDay();
const todaySpecial = specials.find((special) => special.day_of_week === businessWeekday) ?? specials[0];
const welcomePhotos = await getWelcomePhotos(env);
type WelcomeTile =
  | { source: 'client-upload'; photo: WelcomePhoto; menuItem: null }
  | { source: 'menu-fallback'; photo: WelcomePhoto; menuItem: Item };
// Welcome uploads lead; unique active menu photos fill the remainder by category.
const usedWelcomePhotoKeys = new Set<string>();
const usedWelcomeMenuItems = new Set<number>();
const welcomeTiles: WelcomeTile[] = welcomePhotos.flatMap((photo): WelcomeTile[] => {
  // Client uploads lead the photo line and always outrank menu fallbacks.
  if (!photo.photo_key || usedWelcomePhotoKeys.has(photo.photo_key)) return [];
  usedWelcomePhotoKeys.add(photo.photo_key);
  return [{ photo, source: 'client-upload' as const, menuItem: null }];
});

// The menu sequence cycles through each public category before returning for
// another photo, so a long category cannot visually monopolize the marquee.
const categoryPhotoQueues = categories.map((category) => {
  const queue = [] as typeof items;
  for (const item of items) {
    if (item.category_id !== category.id || item.active !== 1 || !item.photo_key || usedWelcomePhotoKeys.has(item.photo_key) || usedWelcomeMenuItems.has(item.id)) continue;
    usedWelcomePhotoKeys.add(item.photo_key);
    usedWelcomeMenuItems.add(item.id);
    queue.push(item);
  }
  return queue;
});
for (let round = 0; categoryPhotoQueues.some((queue) => round < queue.length); round += 1) {
  for (const queue of categoryPhotoQueues) {
    const fallback = queue[round];
    if (!fallback?.photo_key) continue;
    welcomeTiles.push({
      source: 'menu-fallback' as const,
      photo: {
        slot: welcomeTiles.length + 1,
        photo_key: fallback.photo_key,
        alt: fallback.name,
        caption: '',
        orientation: fallback.photo_orientation ?? 'portrait',
      },
      menuItem: fallback,
    });
  }
}
// The Welcome composition has four slots. Keep the display list unique before
// rendering so neither an upload nor a menu fallback can repeat in the collage.
const visibleWelcomeTiles = welcomeTiles.slice(0, 4);
const welcomePhotoSequences = [visibleWelcomeTiles, visibleWelcomeTiles];
---
<Base>
<header class="hero" id="top">
  <h1 class="sr-only">Grayz'n Buffalo Bar &amp; Grill — Mondovi, Wisconsin</h1>
  <div class="hero-copy">
    <div class="logo-stage" aria-hidden="true">
      <img class="logo-layer logo-wordmark" src="/logo-wordmark.webp" alt="" width="900" height="605" decoding="async" />
      <div class="buffalo-video-anchor logo-layer">
        <video
          class="buffalo-video"
          autoplay
          muted
          playsinline
          preload="auto"
          aria-hidden="true"
        >
          <source src="/grok-buffalo-test.webm" type="video/webm" />
        </video>
      </div>
    </div>
  </div>
<div class="opening-poster">
<section class="hero-after">
  <div class="hero-after__layout">
    <article class="daily-special daily-special--lunch">
      <p class="daily-special__label"><span aria-hidden="true">★</span> Lunch Special <span aria-hidden="true">★</span></p>
      <p class="daily-special__hours">11 AM–1:30 PM</p>
      {todaySpecial?.lunch_name && <h2>{todaySpecial.lunch_name}</h2>}
      {todaySpecial?.lunch_description && <p class="daily-special__description">{todaySpecial.lunch_description}</p>}
      {todaySpecial?.lunch_tag && <span class="daily-special__tag">{todaySpecial.lunch_tag}</span>}
    </article>
    <div class="hero-after__brand">
      <div class="logo-bar-stage" aria-hidden="true">
        <img class="logo-bar-grill" src="/logo-bar-grill.webp" alt="" width="900" height="605" decoding="async" />
      </div>
      <div class="hero-actions">
        <p class="hero-tag hero-tag-enter"><span>Open daily 10 AM to 2 AM.</span></p>
      </div>
      <a class="home-specials__cta" href="/specials">View All Specials <span aria-hidden="true">→</span></a>
    </div>
    <article class="daily-special daily-special--dinner">
      <p class="daily-special__label"><span aria-hidden="true">★</span> Dinner Special <span aria-hidden="true">★</span></p>
      <p class="daily-special__hours">5–10 PM</p>
      {todaySpecial?.night_name && <h2>{todaySpecial.night_name}</h2>}
      {todaySpecial?.night_description && <p class="daily-special__description">{todaySpecial.night_description}</p>}
      {todaySpecial?.night_tag && <span class="daily-special__tag">{todaySpecial.night_tag}</span>}
    </article>
  </div>
</section>
</div>
</header>

<section id="welcome" class="section-parchment">
  <div class="welcome-grid">
    <div class="welcome-copy">
      <div class="welcome-title">
        <span class="welcome-title__rule" aria-hidden="true"></span>
        <span class="welcome-title__star" aria-hidden="true">★</span>
        <h2>WELCOME TO GRAYZ'N BUFFALO</h2>
        <span class="welcome-title__star" aria-hidden="true">★</span>
        <span class="welcome-title__rule" aria-hidden="true"></span>
      </div>
      <p class="welcome-intro">A hometown bar &amp; grill in the heart of Mondovi, Wisconsin. We’re about good food, cold drinks, and the people who make this place feel like home.</p>
      <p class="welcome-gumby">Longtime locals knew us as Gumby's. New name, same bar.</p>
    </div>
    {visibleWelcomeTiles.length > 0 && (
      <div class="photo-stack" role="region" aria-label="Welcome and menu photos" tabindex="0">
        <div class="photo-track">
        {welcomePhotoSequences.map((sequence, sequenceIndex) => (
          <div class="photo-sequence" aria-hidden={sequenceIndex === 1 ? 'true' : undefined} inert={sequenceIndex === 1 ? true : undefined}>
          {sequence.map((tile) => {
        const { photo, menuItem } = tile;
        const photoKey = photo.photo_key;
        if (!photoKey) return null;
        const menuAction =
          tile.source === 'menu-fallback' &&
          menuItem?.active === 1 &&
          menuItem.photo_key === photoKey
            ? {
                url: `/menu#menu-item-${menuItem.id}`,
                label: `See ${menuItem.name} on the Menu`,
              }
            : null;
        return (
          <button
            class={`photo photo--${photo.orientation}`}
            type="button"
            data-lightbox-src={`/img/${photoKey}`}
            data-lightbox-alt={photo.alt}
            data-lightbox-action-url={menuAction?.url}
            data-lightbox-action-label={menuAction?.label}
            aria-label={photo.alt ? `View larger photo: ${photo.alt}` : 'View larger photo'}
            tabindex={sequenceIndex === 1 ? -1 : undefined}
          >
            <img src={`/img/${thumbKey(photoKey)}`} alt={photo.alt} loading="lazy" />
            {photo.caption && <span>{photo.caption}</span>}
          </button>
        );
      })}
          </div>
        ))}
        </div>
      </div>
    )}
    <div class="welcome-closing">
      <p class="welcome-script">Pull up a chair. You're among friends here.</p>
    </div>
  </div>
</section>

<section id="menu-preview">
  <div class="section-head menu-heading reveal">
    <span class="menu-heading__rule" aria-hidden="true"></span>
    <span class="menu-heading__star" aria-hidden="true">★</span>
    <h2>The Menu</h2>
    <span class="menu-heading__star" aria-hidden="true">★</span>
    <span class="menu-heading__rule" aria-hidden="true"></span>
    <span class="kicker">From the kitchen</span>
    <p>The daily lineup while the grill's on — and the late-night menu after it shuts down.</p>
  </div>
  <div class="menu-cards">
    <a class="menu-card menu-card--daily reveal" href="/menu">
      <p class="menu-card__eyebrow">Kitchen Window</p>
      <h3>Daily Menu</h3>
      <p>Full kitchen while the grill is running.</p>
    </a>
    <a class="menu-card menu-card--late reveal" href="/menu#late-night">
      <p class="menu-card__eyebrow">After the Grill</p>
      <h3>Late Night</h3>
      <p>After the grill shuts down, the oven keeps going — pizza, pizza fries, appetizers.</p>
    </a>
    <a class="menu-card menu-card--specials reveal" href="/specials">
      <p class="menu-card__eyebrow">Every Day</p>
      <h3>Specials</h3>
      <p>A different deal every day — Mexican Night included.</p>
    </a>
  </div>
</section>

<section id="social" class="home-social" hidden>
  <div class="section-head social-heading reveal">
    <span class="social-heading__rule" aria-hidden="true"></span>
    <span class="social-heading__star" aria-hidden="true">★</span>
    <h2>The Latest</h2>
    <span class="social-heading__star" aria-hidden="true">★</span>
    <span class="social-heading__rule" aria-hidden="true"></span>
  </div>
  <div class="fb-billboard reveal">
    <div class="fb-billboard__top">
      <span class="fb-board-label">Straight from Facebook</span>
    </div>
    <div class="fb-billboard__middle">
      <div class="fb-billboard__middle-slices" aria-hidden="true"></div>
      <div class="fb-grid" id="fbFeed">
        <p style="text-align:center;color:var(--ink-2)">Recent posts appear here once the Facebook feed Worker is connected.</p>
      </div>
    </div>
    <div class="fb-billboard__bottom" aria-hidden="true"></div>
  </div>
  <div class="social-cta reveal">
    <a class="btn btn-primary" href="https://www.facebook.com/grayznbuffalo/">Follow the Grayz'n Buffalo</a>
  </div>
</section>

<Fragment slot="home-footer">
<section id="visit" class="section-tavern">
  <div class="visit-content">
  <h2 class="visit-heading reveal">Come See the Grayz'n Buffalo</h2>
  <span class="kicker visit-kicker reveal">Find us downtown</span>
  <div class="map-frame reveal">
    <iframe title="Map to Grayz'n Buffalo"
      src="https://www.google.com/maps?q=102+S+Eau+Claire+St,+Mondovi,+WI+54755&output=embed"
      loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
  </div>
  <div class="visit-cols reveal">
    <div class="visit-block"><h3>Address</h3><p><a href="https://www.google.com/maps/search/?api=1&query=102+S+Eau+Claire+St,+Mondovi,+WI+54755" target="_blank" rel="noopener">102 S Eau Claire St, Mondovi, WI</a></p></div>
    <div class="visit-block"><h3>Phone</h3><p><a href="tel:+17159264431">(715) 926-4431</a></p></div>
    <div class="visit-block"><h3>Hours</h3><p>Open daily · 10 AM – 2 AM</p></div>
  </div>
  </div>
</section>
</Fragment>
</Base>

<style>
  .hero{--hero-scene-height:min(84dvh,720px);position:relative;z-index:2;isolation:isolate;overflow:hidden;min-height:0;padding-top:var(--hero-scene-height);display:block;text-align:center;background:var(--page-parchment)}
  .hero::before{content:"";position:absolute;z-index:0;inset:0;background:url('/grayznBackground.webp') center bottom / cover no-repeat;mask-image:linear-gradient(to bottom,#000 0%,#000 77%,rgba(0,0,0,.85) 87%,transparent calc(100% + 5px));-webkit-mask-image:linear-gradient(to bottom,#000 0%,#000 77%,rgba(0,0,0,.85) 87%,transparent calc(100% + 5px));pointer-events:none}
  .hero::after{content:none}
  .hero-copy{position:absolute;inset:0;z-index:3;pointer-events:none}
  .logo-stage{--wordmark-y:-105px;--buffalo-y:0px;
    display:block;position:absolute;top:clamp(180px,45vw,210px);left:0;right:0;
    width:min(95%,500px);aspect-ratio:900 / 605;margin:0 auto}
  .logo-layer{position:absolute;top:var(--layer-y);left:0;width:100%;height:100%;pointer-events:none}
  .logo-wordmark{--layer-y:var(--wordmark-y);z-index:2;transform:scale(1.02);transform-origin:center;opacity:0;animation:logo-layer-fade .4s ease 1.25s forwards;will-change:opacity}
  .logo-buffalo-video{--buffalo-scale:.86;display:block;width:100%;height:100%;opacity:1;transform:scale(.2);transform-origin:center bottom;animation:buffalo-scale-charge 1.65s cubic-bezier(.16,.78,.28,1) forwards;animation-fill-mode:forwards;backface-visibility:hidden;will-change:transform}
  .logo-bar-stage{display:flex;align-items:center;justify-content:center;position:relative;width:min(80vw,360px);height:clamp(90px,20vw,150px);margin:0 auto;pointer-events:none}
  .logo-bar-grill{display:block;width:100%;height:auto;transform:translateY(5px);opacity:0;pointer-events:none;animation:logo-bar-fade .4s ease 1.65s forwards;will-change:opacity}
  .hero-after{padding:0;background:var(--cream)}
  .hero-actions{margin-top:-30px;padding:clamp(18px,4vw,28px) 24px clamp(30px,5vw,44px);text-align:center}
  .hero-tag{font-size:clamp(1.05rem,2.4vw,1.4rem);color:var(--ink);
    font-weight:600;letter-spacing:.01em}
  @media (min-width:1100px){
    .hero{--hero-scene-height:542px;min-height:0}
    .hero-copy{position:absolute;inset:0;max-width:none;margin:0;padding:0}
    .logo-stage{--wordmark-y:-74px;--buffalo-y:8px;
      display:block;position:absolute;top:190px;left:0;right:0;width:min(78vw,560px);aspect-ratio:900 / 605;margin:0 auto;pointer-events:none}
    .logo-wordmark{--layer-y:var(--wordmark-y);z-index:2;transform:scale(1.23);transform-origin:center}
    
    .logo-buffalo{--buffalo-scale:.8}
    .logo-wordmark{opacity:0;animation:logo-layer-fade .4s ease 1.25s forwards;will-change:opacity}
    .logo-bar-stage{display:flex;align-items:center;justify-content:center;position:relative;width:min(50vw,400px);height:150px;margin:0 auto;pointer-events:none}
    .logo-bar-grill{display:block;width:100%;height:auto;transform:translateY(2px);opacity:0;pointer-events:none;animation:logo-bar-fade .4s ease 1.65s forwards;will-change:opacity}
  }
  .hero-tag-enter{opacity:0;animation:hero-tag-fade .4s ease 2.15s forwards}
  @keyframes hero-tag-fade{to{opacity:1}}
  .buffalo-video-anchor {
    --layer-y: var(--buffalo-y);
    z-index: 1;
    display: flex;
    align-items: flex-end;
    justify-content: center;
  }

  .buffalo-video {
    display: block;
    width: 46%;
    height: auto;
    object-fit: contain;
  }
  @keyframes logo-layer-fade{to{opacity:1}}
  @keyframes logo-bar-fade{to{opacity:1}}
  .dark{background:var(--ink);color:var(--cream)}
  #welcome{padding-block:clamp(34px,5vw,58px)}
  .welcome-grid{display:grid;grid-template-columns:minmax(0,.8fr) minmax(420px,1.2fr);gap:clamp(24px,3vw,42px);max-width:1240px;margin:0 auto;align-items:start}
  .welcome-copy{max-width:38ch}
  .welcome-copy h2{text-align:left}
  .welcome-copy .kicker{display:block;text-align:left}
  .welcome-copy p{margin:14px 0;color:var(--ink-2)}
  .photo-stack{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));grid-auto-rows:clamp(160px,16vw,220px);gap:12px;align-content:start}
  .photo{height:100%;max-height:220px;min-height:0;aspect-ratio:4/3;border:0;border-radius:12px;position:relative;overflow:hidden;
    background:var(--cream-2);
    box-shadow:0 10px 26px rgba(36,22,16,.22);display:flex;align-items:flex-end;transition:transform .3s ease;cursor:zoom-in}
  .photo.welcome-photo-reveal{opacity:0;transform:translateX(56px);transition:opacity .65s ease,transform .65s cubic-bezier(.19,.74,.36,1)}
  .photo.welcome-photo-reveal.in{opacity:1;transform:translateX(0)}
  .photo.welcome-photo-reveal:nth-child(2){transition-delay:.14s}
  .photo.welcome-photo-reveal:nth-child(3){transition-delay:.28s}
  .photo.welcome-photo-reveal:nth-child(4){transition-delay:.42s}
  .photo img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:saturate(.84) sepia(.08) contrast(.94) brightness(1.04)}
  .photo:hover{transform:translateY(-6px)}
  .photo.welcome-photo-reveal.in:hover{transform:translateY(-6px)}
  .photo:focus-visible{outline:3px solid var(--gold);outline-offset:4px}
  .photo span{position:relative;width:100%;padding:10px 12px;font-family:var(--label);font-weight:600;font-size:.8rem;
    letter-spacing:.12em;text-transform:uppercase;color:var(--cream);
    background:linear-gradient(transparent,rgba(36,22,16,.78))}
  .menu-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:22px;max-width:1120px;margin:0 auto}
  .menu-card{background:var(--cream);border-radius:14px;padding:30px 26px;border-top:6px solid var(--crimson);
    box-shadow:0 8px 22px rgba(36,22,16,.12);transition:transform .25s ease, box-shadow .25s ease}
  .menu-card:nth-child(2){border-top-color:var(--amber)}
  .menu-card:nth-child(3){border-top-color:var(--denim)}
  .menu-card:hover{transform:translateY(-7px);box-shadow:0 16px 34px rgba(36,22,16,.18)}
  .menu-card h3{font-family:var(--display);font-size:1.35rem;margin-bottom:8px}
  .menu-card p{color:var(--ink-2);font-size:.98rem;margin-bottom:16px}
  .link-arrow{font-family:var(--label);font-weight:700;letter-spacing:.14em;text-transform:uppercase;
    color:var(--btn-crimson);text-decoration:none;font-size:.92rem}
  .link-arrow:hover{text-decoration:underline}
  .section-head p a{color:var(--btn-crimson);font-weight:700;text-underline-offset:3px;text-decoration-thickness:1.5px}
  .section-head p a:hover,.section-head p a:focus-visible{color:var(--ink);text-decoration-thickness:2px}
  #social .section-head .kicker,#menu-preview .section-head .kicker{display:block;margin:-2px 0 10px}
  .visit-heading{color:var(--gold);text-align:center;max-width:900px;margin:0 auto 32px}
  .map-frame{border-radius:14px;width:100%;max-width:1100px;height:clamp(300px,42vw,440px);
    margin:0 auto;overflow:hidden;border:2px solid rgba(247,234,211,.2)}
  .map-frame iframe{display:block;width:100%;height:100%;border:0}
  .visit-cols{
    display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(20px,4vw,48px);
    max-width:900px;margin:38px auto 0;text-align:center;
  }
  .visit-block h3{font-family:var(--label);font-weight:700;letter-spacing:.2em;text-transform:uppercase;font-size:.85rem;color:var(--amber);margin-bottom:6px}
  .visit-block p{color:var(--cream-2);white-space:nowrap}
  .visit-block:first-child p{white-space:normal}
  .visit-block a{color:var(--gold);text-decoration:underline;text-underline-offset:3px;text-decoration-thickness:1.5px;}
  .visit-block a:hover,
  .visit-block a:focus-visible{color:var(--cream);text-decoration-thickness:2px;}
  @media (min-width:1100px) {
    .buffalo-video {
      width: 42%;
    }
  }
  @media (max-width:900px){ .welcome-grid{grid-template-columns:1fr} .welcome-copy{max-width:52ch}.photo-stack{grid-template-columns:1fr;grid-auto-rows:clamp(180px,62vw,280px)}.photo-stack .photo{max-height:280px} }
  @media (max-width:800px){ .visit-cols{grid-template-columns:1fr;gap:20px} .visit-block p{white-space:normal} }
  @media (max-width:1099px){
    .hero{--hero-scene-height:clamp(460px,55svh,500px);min-height:0}
    .logo-bar-stage{height:clamp(90px,20vw,150px)}
    .hero-actions{padding:0 20px 24px}
  }
  @media (max-width:480px){
    .hero{--hero-scene-height:max(clamp(460px,55svh,480px),min(109vw,522px));min-height:0}
    .logo-bar-stage{width:min(65vw,280px);height:auto}
    .hero-actions{margin-top:0;padding-top:24px}
  }
  @media (prefers-reduced-motion: reduce){
    .hero-tag-enter{animation:none;opacity:1;transform:none}
      .buffalo-anchor{animation:none;transform:none}
      .buffalo-video-anchor {
      display: none;
    }
    .logo-buffalo{animation:none;opacity:1;transform:scale(var(--buffalo-scale))}
    .logo-wordmark{animation:none;opacity:1}
    .logo-bar-grill{animation:none;opacity:1}
    .photo.welcome-photo-reveal{opacity:1;transform:none;transition:none}
  }
  /* ---- Homepage vintage poster system ---- */
  #welcome{padding-block:clamp(42px,6vw,72px);background:var(--page-parchment)}
  .welcome-grid{grid-template-columns:minmax(0,.62fr) minmax(0,1.38fr);gap:clamp(28px,5vw,64px);max-width:var(--content-wide);align-items:start}
  .welcome-copy{max-width:34ch;padding-top:clamp(4px,1vw,16px)}
  .welcome-title{display:grid;grid-template-columns:minmax(18px,1fr) auto auto auto minmax(18px,1fr);align-items:center;gap:clamp(7px,1vw,12px);margin:8px 0 16px;color:var(--brick)}
  .welcome-title h2{font-family:var(--display);font-size:clamp(1.7rem,3vw,2.7rem);line-height:1.04;text-align:center}
  .welcome-title__star{font-size:clamp(.9rem,1.5vw,1.2rem);line-height:1}
  .welcome-title__rule{height:2px;background:currentColor;opacity:.62}
  .welcome-copy p{margin:12px 0;color:var(--text-muted);font-size:1.04rem}
  .photo-stack{grid-template-columns:.95fr 1.15fr .95fr;grid-auto-rows:clamp(210px,20vw,270px);gap:0;max-width:800px;padding:16px 22px 28px}
  .photo{max-height:none;aspect-ratio:auto;border:2px solid var(--ink);border-radius:var(--panel-radius);background:var(--surface-aged);box-shadow:var(--panel-shadow)}
  .photo img{object-fit:cover}
  .photo:hover,.photo.welcome-photo-reveal.in:hover{transform:translateY(-3px)}
  .photo span{padding:8px 10px;font-size:.72rem;letter-spacing:.1em}

  .home-specials__cta{display:inline-flex;gap:10px;align-items:center;margin-top:22px;color:var(--ink);font-family:var(--label);font-weight:700;font-size:.84rem;letter-spacing:.15em;text-transform:uppercase;text-decoration:none;border-bottom:2px solid var(--brick);padding-bottom:3px;transition:color .2s ease,border-color .2s ease}
  .home-specials__cta:hover,.home-specials__cta:focus-visible{color:var(--gold);border-bottom-color:var(--gold)}

  #menu-preview{padding-block:clamp(44px,6vw,76px);background:var(--surface-aged)}
  #menu-preview .section-head{margin-bottom:30px}
  .menu-cards{grid-template-columns:repeat(3,minmax(0,1fr));gap:14px;max-width:var(--content-wide)}
  .menu-card{min-height:250px;border:2px solid var(--ink);border-radius:var(--panel-radius);padding:clamp(22px,3vw,34px) clamp(20px,2.5vw,30px);box-shadow:var(--panel-shadow);display:flex;flex-direction:column;align-items:flex-start;justify-content:flex-start;transition:transform .2s ease,box-shadow .2s ease}
  .menu-card::before{content:"Kitchen window";font-family:var(--label);font-weight:700;font-size:.72rem;letter-spacing:.17em;text-transform:uppercase;line-height:1.2;padding-bottom:10px;margin-bottom:16px;border-bottom:2px solid currentColor;width:100%}
  .menu-card--daily{background:var(--surface-parchment);color:var(--ink)}
  .menu-card--late{background:var(--surface-wood);border-color:var(--ochre);color:var(--cream)}
  .menu-card--late::before{content:"After the grill";color:var(--ochre)}
  .menu-card--specials{background:var(--brick);border-color:var(--ink);color:var(--cream)}
  .menu-card--specials::before{content:"Every day";color:var(--cream)}
  .menu-card h3{font-size:clamp(1.35rem,2.2vw,1.8rem);line-height:1.1;margin-bottom:10px}
  .menu-card p{color:currentColor;opacity:.84;margin-bottom:20px;line-height:1.4}
  .menu-card .link-arrow{margin-top:auto;color:currentColor;font-size:.84rem;text-decoration:underline;text-underline-offset:4px;text-decoration-thickness:2px}
  .menu-card:hover{transform:translateY(-3px);box-shadow:0 6px 0 rgba(43,27,20,.18)}

  .home-social{padding-block:clamp(44px,6vw,76px);background:var(--page-parchment)}
  .home-social .section-head{margin-bottom:30px}
  .social-cta{text-align:center;margin-top:28px}

  #visit{display:flex;flex-direction:column;gap:22px;padding-block:clamp(42px,6vw,72px);background:var(--surface-wood)}
  .visit-heading{order:1;max-width:var(--content-wide);width:100%;margin:0 auto;color:var(--cream);text-align:left;font-size:clamp(2rem,3.6vw,3.1rem)}
  .visit-kicker{order:2;align-self:flex-start;color:var(--ochre);max-width:var(--content-wide);width:100%;margin-inline:auto}
  .visit-cols{order:3;display:grid;grid-template-columns:1fr;gap:14px;max-width:none;width:100%;margin:0;text-align:left}
  .visit-block{padding:0 0 13px;border-bottom:1px solid var(--rule-light)}
  .visit-block h3{color:var(--ochre);margin-bottom:2px;letter-spacing:.15em}
  .visit-block p,.visit-block:first-child p{color:var(--cream);white-space:normal;margin:0}
  .visit-block a{color:var(--cream);text-decoration-color:var(--ochre)}
  .map-frame{order:4;width:100%;max-width:none;height:clamp(250px,46vw,360px);margin:0;border:2px solid var(--ochre);border-radius:var(--panel-radius);box-shadow:var(--panel-shadow-dark)}

  @media (min-width:900px){
    #visit{display:grid;grid-template-columns:minmax(280px,.82fr) minmax(0,1.18fr);column-gap:clamp(32px,6vw,76px);align-items:start}
    .visit-kicker,.visit-heading{grid-column:1;width:auto;margin:0}
    .visit-cols{grid-column:1;grid-row:3}
    .map-frame{grid-column:2;grid-row:1 / span 3;height:100%;min-height:330px}
  }
  @media (max-width:900px){
    .welcome-grid{grid-template-columns:1fr;gap:24px}
    .welcome-copy{max-width:54ch}
    .photo-stack{grid-template-columns:repeat(2,minmax(0,1fr));grid-auto-rows:clamp(160px,46vw,240px);max-width:660px;margin-inline:auto}
    .photo:nth-child(3){grid-column:1 / -1;width:58%;justify-self:center}
  }
  @media (max-width:620px){
    .menu-cards{grid-template-columns:1fr;gap:12px}
    .menu-card{min-height:0}
    .menu-card p{margin-bottom:14px}
    .welcome-title{grid-template-columns:minmax(16px,1fr) auto auto auto minmax(16px,1fr);gap:6px}
    .welcome-title h2{font-size:clamp(1.4rem,7.1vw,1.8rem)}
  }

  /* Printed paper, posted photos, and railroad-stationery surfaces. */
  .hero-after,#welcome{position:relative;background-color:var(--page-parchment);background-image:radial-gradient(rgba(77,45,24,.1) .65px,transparent .75px),linear-gradient(90deg,rgba(111,65,29,.07),transparent 15%,transparent 85%,rgba(111,65,29,.06));background-size:5px 5px,100% 100%}
  .welcome-copy h2{font-family:var(--display);line-height:1.05}.welcome-copy p{max-width:35ch}
  .photo-stack{gap:0;padding:16px 22px 28px}
  .photo{border:8px solid #f6ecd8;border-radius:0;box-shadow:2px 3px 0 rgba(76,45,24,.22),7px 10px 18px rgba(43,27,20,.2);overflow:visible;background:#f6ecd8}
  .photo img{inset:0;width:100%;height:100%;border:1px solid rgba(57,35,21,.32)}
  .photo:nth-child(1){z-index:1;transform:rotate(-3deg) translateY(18px);margin-right:-28px}.photo:nth-child(2){z-index:2;transform:rotate(1.8deg) translateY(-7px);margin-inline:-18px}.photo:nth-child(3){z-index:1;transform:rotate(-1.7deg) translateY(22px);margin-left:-28px}
  .photo:hover,.photo.welcome-photo-reveal.in:hover{transform:translateY(-5px) rotate(0)}
  #menu-preview{position:relative;isolation:isolate;background-color:#493222;background-image:linear-gradient(rgba(27,16,10,.22),rgba(27,16,10,.22)),url('/images/menu/menu-wood.webp');background-size:100% 100%,860px 860px;background-repeat:no-repeat,repeat;border:0;box-shadow:inset 0 1px rgba(246,220,169,.1),inset 0 -1px rgba(14,8,5,.45)}
  #menu-preview::before,#menu-preview::after{content:"";position:absolute;z-index:0;right:0;left:0;height:9px;pointer-events:none;background:linear-gradient(to bottom,rgba(24,14,9,.96) 0 1px,rgba(211,169,100,.42) 1px 2px,transparent 2px 5px,rgba(20,12,8,.74) 5px 6px,rgba(238,206,145,.2) 6px 7px,rgba(23,14,9,.9) 7px 9px)}
  #menu-preview::before{top:0}#menu-preview::after{bottom:0;transform:scaleY(-1)}#menu-preview > *{position:relative;z-index:1}
  #menu-preview .section-head :is(h1,h2){color:var(--cream)}#menu-preview .section-head p{color:var(--cream-2)}#menu-preview .section-head .kicker{color:#e3bd75}
  /* Generated paper sheets provide the aging, torn edges, printed border, and ornaments. */
  .menu-card{--menu-poster:url('/images/menu/menu-poster-daily.webp');--poster-tilt:-1deg;--poster-hover-tilt:-.3deg;position:relative;isolation:isolate;display:flex;flex-direction:column;align-items:flex-start;aspect-ratio:4 / 3;min-height:0;padding:clamp(34px,5vw,55px) clamp(30px,4.4vw,48px) clamp(37px,5.4vw,58px);overflow:visible;border:0;border-radius:0;background:var(--menu-poster) center / 100% 100% no-repeat;box-shadow:none;cursor:pointer;filter:drop-shadow(2px 3px 2px rgba(18,10,6,.38)) drop-shadow(7px 11px 14px rgba(18,10,6,.32));color:var(--ink);text-decoration:none;transform:rotate(var(--poster-tilt));transition:transform .2s ease,filter .2s ease}
  .menu-card::before,.menu-card::after{content:none}
  .menu-card__eyebrow{margin:0 0 10px;color:#69401e;font-family:var(--label);font-size:.76rem;font-weight:700;letter-spacing:.2em;line-height:1.2;text-transform:uppercase}
  .menu-card h3{margin:0 0 10px;color:#3c2416;font-family:var(--display);font-size:clamp(1.32rem,2.1vw,1.82rem);line-height:1.08;text-transform:uppercase}
  .menu-card p:not(.menu-card__eyebrow){margin:0;color:#503a2c;font-family:var(--body);font-size:clamp(.9rem,1.2vw,1rem);line-height:1.38}
  .menu-card--late{--menu-poster:url('/images/menu/menu-poster-late-night.webp');--poster-tilt:.8deg;--poster-hover-tilt:.22deg}
  .menu-card--specials{--menu-poster:url('/images/menu/menu-poster-specials.webp');--poster-tilt:-.6deg;--poster-hover-tilt:-.18deg}
  .menu-card--daily,.menu-card--specials{padding-top:28%}
  .menu-card.reveal.in{transform:rotate(var(--poster-tilt))}
  .menu-card:hover,.menu-card:focus-visible,.menu-card.reveal.in:hover,.menu-card.reveal.in:focus-visible{z-index:2;transform:translateY(-7px) rotate(var(--poster-tilt));filter:drop-shadow(2px 7px 4px rgba(18,10,6,.46)) drop-shadow(12px 24px 28px rgba(18,10,6,.48))}
  .menu-card:focus-visible{outline:3px solid var(--gold);outline-offset:6px}
  @media (max-width:620px){.menu-card{--poster-tilt:-.45deg;--poster-hover-tilt:-.12deg;padding:clamp(33px,11vw,45px) clamp(28px,10vw,42px) clamp(35px,12vw,49px)}.menu-card--late{--poster-tilt:.4deg;--poster-hover-tilt:.1deg}.menu-card--specials{--poster-tilt:-.3deg;--poster-hover-tilt:-.08deg}.menu-card__eyebrow{margin-bottom:7px;font-size:.68rem}.menu-card h3{font-size:clamp(1.18rem,6vw,1.58rem)}.menu-card p:not(.menu-card__eyebrow){font-size:clamp(.82rem,3.7vw,.94rem)}}
  @media (prefers-reduced-motion:reduce){.menu-card{transition:none}.menu-card:hover,.menu-card:focus-visible,.menu-card.reveal.in:hover,.menu-card.reveal.in:focus-visible{transform:rotate(var(--poster-tilt))}}
  .home-social{background-color:var(--surface-aged);background-image:radial-gradient(rgba(77,45,24,.08) .6px,transparent .75px);background-size:5px 5px;border-top:1px solid var(--rule);border-bottom:1px solid var(--rule)}
  #visit{position:relative;isolation:isolate;background-color:var(--surface-parchment);background-image:linear-gradient(90deg,rgba(86,48,25,.12) 0 1px,transparent 1px 30px),linear-gradient(0deg,rgba(86,48,25,.09) 0 1px,transparent 1px 30px),linear-gradient(105deg,#f3e5c9,#d7bd8d);color:var(--ink);border:0}
  #visit::after{content:'';position:absolute;z-index:-1;right:clamp(10px,6vw,90px);bottom:-18px;width:min(42vw,360px);height:260px;background:url('/logo-buffalo.webp') center / contain no-repeat;opacity:.075;filter:sepia(1) contrast(.8)}
  .visit-heading{color:var(--ink);font-family:var(--display)}.visit-kicker{color:var(--brick)}.visit-cols{border-top:0;padding-top:16px}.visit-block{border-bottom:1px dashed var(--rule)}.visit-block h3{color:var(--brick)}.visit-block p,.visit-block:first-child p,.visit-block a{color:var(--ink)}
  .map-frame{border:6px solid #f7ecd6;border-radius:0;box-shadow:3px 3px 0 rgba(43,27,20,.3),9px 12px 22px rgba(43,27,20,.14)}
  /* The content group is separate from its scene so desktop can move it without moving the shared landscape. */
  #visit{display:block;padding:0;background:transparent}
  #visit::after{display:none}
  .visit-content{display:flex;flex-direction:column;gap:22px;padding:clamp(42px,6vw,72px) clamp(18px,4vw,64px);background-color:var(--surface-parchment);background-image:linear-gradient(90deg,rgba(86,48,25,.12) 0 1px,transparent 1px 30px),linear-gradient(0deg,rgba(86,48,25,.09) 0 1px,transparent 1px 30px),linear-gradient(105deg,#f3e5c9,#d7bd8d)}
  .visit-content .visit-heading{order:1;max-width:var(--content-wide);width:100%;margin:0 auto;text-align:left;font-size:clamp(2rem,3.6vw,3.1rem)}
  .visit-content .visit-kicker{order:2;align-self:flex-start;max-width:var(--content-wide);width:100%;margin-inline:auto}
  .visit-content .visit-cols{order:3;display:grid;grid-template-columns:1fr;gap:14px;max-width:none;width:100%;margin:0;text-align:left}
  .visit-content .visit-block{padding:0 0 13px;border-bottom:1px solid var(--rule-light)}
  .visit-content .visit-block a:hover,.visit-content .visit-block a:focus-visible{color:#21130e;font-weight:700;text-decoration-thickness:2px}
  .visit-content .map-frame{order:4;width:100%;max-width:none;height:clamp(250px,46vw,360px);margin:0}
  @media (min-width:900px){.visit-content{display:grid;grid-template-columns:minmax(280px,.82fr) minmax(0,1.18fr);column-gap:clamp(32px,6vw,76px);align-items:start}.visit-content .visit-kicker,.visit-content .visit-heading{grid-column:1;width:auto;margin:0}.visit-content .visit-cols{grid-column:1;grid-row:3}.visit-content .map-frame{grid-column:2;grid-row:1 / span 3;height:100%;min-height:330px}}

  /* The intro reads as one posted-paper composition: daily boards flank the mark. */
  .hero-after{padding:clamp(26px,4vw,52px) 24px clamp(22px,3vw,36px);text-align:center}
  .hero-after__layout{display:grid;grid-template-columns:minmax(220px,.9fr) minmax(300px,1.28fr) minmax(220px,.9fr);grid-template-areas:"lunch brand dinner";align-items:stretch;gap:clamp(20px,3.5vw,52px);max-width:var(--content-wide);margin:0 auto}
  .daily-special{position:relative;isolation:isolate;display:flex;flex-direction:column;justify-content:center;min-width:0;min-height:220px;padding:clamp(22px,2.8vw,34px) clamp(18px,2.5vw,30px);background-color:#173747;background-image:radial-gradient(rgba(247,236,214,.13) .65px,transparent .8px),linear-gradient(115deg,rgba(0,0,0,.16),transparent 22%,transparent 76%,rgba(0,0,0,.13));background-size:5px 5px,100% 100%;color:#f6ecd8;border:2px solid var(--ochre);box-shadow:4px 5px 0 rgba(43,27,20,.28),0 10px 22px rgba(43,27,20,.2);text-align:center}
  .daily-special::before{content:"";position:absolute;z-index:-1;inset:6px;border:1px solid rgba(235,194,109,.62);pointer-events:none}
  .daily-special--lunch{grid-area:lunch;border-top:2px solid var(--ochre)}
  .daily-special--dinner{grid-area:dinner;border-top:2px solid var(--ochre)}
  .daily-special__label{display:flex;justify-content:center;gap:10px;margin:0;color:#f6ecd8;font-family:var(--label);font-weight:700;font-size:clamp(.8rem,1.2vw,.95rem);letter-spacing:.14em;text-transform:uppercase;line-height:1.25}
  .daily-special__label span{display:inline;margin:0;color:var(--ochre);font-size:.88em;letter-spacing:0}
  .daily-special__hours{margin:6px 0 14px;padding-bottom:11px;border-bottom:1px dashed rgba(246,236,216,.64);color:#ead9b9;font-family:var(--label);font-size:.72rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase}
  .daily-special h2{margin:0 0 8px;color:#f8efd8;font-family:var(--display);font-size:clamp(1.35rem,2.15vw,2rem);line-height:1.05}
  .daily-special__description{margin:0 auto;color:#f6ecd8;line-height:1.35}
  .daily-special__tag{display:inline-block;margin-top:14px;padding:4px 9px;border:1px solid rgba(235,194,109,.75);color:var(--ochre);font-family:var(--label);font-size:.7rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;background:transparent}
  .hero-after__brand{grid-area:brand;align-self:center;min-width:0}
  .hero-after__brand .logo-bar-stage{width:min(100%,400px);height:150px}
  .hero-after__brand .hero-actions{margin-top:-26px;padding:14px 10px 0}
  .hero-after .home-specials__cta{margin-top:clamp(18px,2.5vw,28px);color:var(--ink)}

  #welcome{padding:clamp(40px,6vw,72px) 24px clamp(48px,7vw,84px);border-bottom:1px solid rgba(91,48,27,.25)}
  .welcome-grid{display:block;max-width:var(--content-wide);margin:0 auto}
  .welcome-copy{max-width:var(--content-wide);margin:0 auto;text-align:center;padding:0}
  .welcome-copy p{max-width:62ch;margin:0 auto;color:var(--text-muted);font-size:clamp(1rem,1.45vw,1.15rem);line-height:1.45}
  .welcome-title{margin:clamp(18px,2.5vw,28px) 0 12px}
  .welcome-copy .welcome-title + p{font-size:clamp(.95rem,1.25vw,1.04rem)}
  .photo-stack{grid-template-columns:.95fr 1.15fr .95fr;grid-auto-rows:clamp(220px,22vw,310px);max-width:900px;margin:clamp(20px,3vw,32px) auto 0;padding:16px 34px 28px}

  @media (max-width:900px){
    .hero-after__layout{grid-template-columns:1fr;grid-template-areas:"brand" "lunch" "dinner";max-width:520px;gap:20px}
    .hero-after__brand{padding-bottom:2px}
    .daily-special{padding:26px 28px}
    .photo-stack{grid-template-columns:repeat(2,minmax(0,1fr));grid-auto-rows:clamp(170px,48vw,260px);max-width:660px}
  }
  @media (max-width:620px){
    .hero-after{padding-inline:16px}
    .hero-after__brand .logo-bar-stage{width:min(65vw,280px);height:auto}
    .hero-after__brand .hero-actions{margin-top:0;padding-top:18px}
    #welcome{padding-inline:16px}
    .welcome-copy p{max-width:36ch}
    .photo-stack{padding-inline:20px}
  }

  /* Final poster proportions: the center returns to its pre-composition mark scale. */
  .opening-poster{position:relative;z-index:1;isolation:isolate;overflow:visible;margin-top:0}
  .hero-after{isolation:isolate;overflow:hidden;padding:clamp(14px,2.25vw,28px) clamp(18px,4vw,64px) clamp(20px,3vw,32px);background:transparent;background-image:none}
  .hero-after__layout{position:relative;z-index:2;grid-template-columns:minmax(280px,1fr) minmax(400px,460px) minmax(280px,1fr);gap:clamp(32px,4vw,64px);max-width:1360px}
  .hero-after__brand{position:relative;isolation:isolate}
  .hero-after__brand::before{content:"";position:absolute;z-index:0;inset:48px -14% -4px;background:radial-gradient(ellipse at 50% 46%,color-mix(in srgb,var(--cream) 64%,transparent) 0%,color-mix(in srgb,var(--cream) 36%,transparent) 50%,transparent 74%);filter:blur(8px);opacity:0;pointer-events:none;animation:logo-bar-haze-fade .4s ease 1.65s forwards}
  .logo-bar-stage,.hero-after__brand .hero-actions,.hero-after__brand .home-specials__cta{position:relative;z-index:1}
  .logo-bar-grill{position:relative;z-index:1}
  @keyframes logo-bar-haze-fade{to{opacity:1}}
  .daily-special{min-height:300px;padding:clamp(26px,3vw,38px) clamp(22px,2.75vw,34px)}
  .hero-after__brand{align-self:start}
  .hero-after__brand .logo-bar-stage{width:min(50vw,400px);height:150px}
  .hero-after__brand .hero-actions{display:flex;flex:1;align-items:center;justify-content:center;margin-top:0;padding:0 24px 8px}
  .hero-after__brand .hero-tag{margin:0}
  .hero-tag span{display:block}
  .hero-after__brand .home-specials__cta{margin-top:4px}

  .welcome-title{grid-template-columns:minmax(24px,1fr) auto auto auto minmax(24px,1fr);width:100%;margin:0 0 16px}
  .welcome-title h2{font-size:clamp(1.55rem,2.65vw,2.5rem);white-space:nowrap}
  .welcome-intro{max-width:62ch!important}
  .photo-stack{grid-template-columns:repeat(5,minmax(0,1fr));grid-auto-rows:clamp(170px,16vw,238px);gap:10px;max-width:1100px;margin:clamp(22px,3vw,34px) auto 0;padding:20px 32px 30px}
  .photo{margin:0!important;width:auto!important;grid-column:auto!important;justify-self:stretch!important}
  .photo:nth-child(1){z-index:1;transform:rotate(-2.8deg) translateY(12px)}
  .photo:nth-child(2){z-index:2;transform:rotate(2deg) translateY(-5px)}
  .photo:nth-child(3){z-index:1;transform:rotate(-1.9deg) translateY(15px)}
  .photo:nth-child(4){z-index:2;transform:rotate(2.5deg) translateY(-8px)}
  .photo:nth-child(5){z-index:1;transform:rotate(-2.2deg) translateY(10px)}
  .welcome-closing{max-width:660px;margin:14px auto 0;text-align:center;color:var(--text-muted)}
  .welcome-closing > p:first-child{margin:0;font-size:clamp(.95rem,1.3vw,1.08rem)}
  .welcome-script{margin:10px 0 0;color:var(--denim);font-family:var(--body);font-size:clamp(1.25rem,2.1vw,1.7rem);font-style:italic;font-weight:600;line-height:1.2}
  .welcome-flourish{display:block;margin:4px auto 0;color:var(--brick);font-size:2rem;line-height:1;transform:rotate(-12deg)}

  @media (max-width:900px){
    .hero-after__layout{grid-template-columns:1fr;grid-template-areas:"brand" "lunch" "dinner";max-width:560px;gap:20px}
    .hero-after__brand{align-self:auto}
    .daily-special{min-height:220px}
    .photo-stack{grid-template-columns:repeat(2,minmax(0,1fr));grid-auto-rows:clamp(170px,48vw,260px);max-width:660px}
    .photo:nth-child(5){grid-column:1 / -1;width:58%!important;justify-self:center!important}
  }
  @media (max-width:620px){ .welcome-title h2{white-space:normal} .photo-stack{padding-inline:20px} }

  /* The center poster aligns to the boards; the link uses the lower breathing room. */
  .hero-after__brand{height:300px;display:flex;flex-direction:column}
  .hero-after__brand .logo-bar-stage{transform:translateY(72px)}
  .hero-after__brand .home-specials__cta{margin-top:auto;margin-bottom:0;align-self:center}
   @media (min-width:1100px){ .hero-after__brand .hero-actions{margin-top:36px} }
  .daily-special{transition:transform .22s ease,box-shadow .22s ease,filter .22s ease}
  .daily-special:hover{transform:translateY(-6px) rotate(-.3deg);box-shadow:6px 9px 0 rgba(43,27,20,.26),0 16px 28px rgba(43,27,20,.25);filter:brightness(1.07)}
  .daily-special--dinner:hover{transform:translateY(-6px) rotate(.3deg)}

  .welcome-gumby{margin-top:clamp(18px,2.5vw,28px)!important;font-size:clamp(.95rem,1.25vw,1.04rem)!important}
  .photo-stack{display:block;width:100vw;max-width:none;margin:clamp(24px,3vw,36px) 0 0 calc(50% - 50vw);padding:40px 0 48px;overflow:hidden}
  .photo-track{--marquee-entry-offset:clamp(220px,22vw,340px);display:flex;width:max-content;animation:welcome-photo-marquee 105s linear 5.7s both infinite}
  .photo-sequence{display:flex;align-items:center;gap:clamp(12px,1.8vw,24px);padding-right:clamp(12px,1.8vw,24px);flex:none}
  .photo{flex:none;display:block;width:clamp(180px,15vw,245px)!important;height:clamp(160px,14vw,220px);margin:0!important;overflow:visible;transform:none!important;transition:transform .2s ease,box-shadow .2s ease}
  .photo--landscape{width:clamp(220px,19vw,300px)!important}.photo--square{width:clamp(175px,14vw,230px)!important}.photo--portrait{width:clamp(160px,12vw,195px)!important}
  .photo img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}
  .photo:nth-child(odd){transform:rotate(-3.1deg) translateY(13px)!important}.photo:nth-child(even){transform:rotate(2.7deg) translateY(-9px)!important}
  .photo:nth-child(3n){transform:rotate(-1.7deg) translateY(3px)!important}.photo:nth-child(4n){transform:rotate(3.5deg) translateY(-15px)!important}
  .photo:hover{transform:translateY(-12px) rotate(0)!important;z-index:4;box-shadow:3px 5px 0 rgba(76,45,24,.25),10px 14px 22px rgba(43,27,20,.26)}
  .welcome-closing{margin:10px auto 0;padding-bottom:clamp(18px,3vw,32px)}
  .welcome-flourish{display:none}
  .photo{background-color:#ead9b7;background-image:radial-gradient(rgba(89,57,31,.11) .6px,transparent .8px);background-size:4px 4px}
  .photo::after{content:"";position:absolute;pointer-events:none;inset:-5px;border:1px solid rgba(112,72,35,.2);opacity:.55;transform:rotate(.2deg)}
  .photo-stack[data-lightbox-paused="true"] .photo-track{animation-play-state:paused}
  @keyframes welcome-photo-marquee{from{transform:translateX(calc(-1 * var(--marquee-entry-offset)))}to{transform:translateX(calc(-50% - var(--marquee-entry-offset)))}}

  /* The lower poster assembles after the hero mark: boards settle with the second line of copy, then Welcome follows. */
  .hero-after,#welcome{overflow-x:clip}
  .welcome-grid{position:relative;z-index:2}
  .hero-after{overflow:visible}
  .daily-special--lunch,.daily-special--dinner{opacity:0;will-change:transform,opacity}
  .daily-special--lunch{animation:poster-board-left .9s cubic-bezier(.2,.72,.25,1) 1.65s both}
  .daily-special--dinner{animation:poster-board-right .9s cubic-bezier(.2,.72,.25,1) 1.65s both}
  @keyframes poster-board-left{from{opacity:0;transform:translateX(calc(-100% - 8vw))}to{opacity:1;transform:translateX(0)}}
  @keyframes poster-board-right{from{opacity:0;transform:translateX(calc(100% + 8vw))}to{opacity:1;transform:translateX(0)}}

  .welcome-title h2,.welcome-title__rule,.welcome-title__star,.welcome-intro,.welcome-gumby,.photo-stack,.welcome-closing{opacity:0;will-change:transform,opacity}
  .hero-after .home-specials__cta{opacity:0;transform:translateY(6px);will-change:transform,opacity;animation:poster-cta .45s ease 2.58s both}
  @keyframes poster-cta{to{opacity:1;transform:translateY(0)}}
  .welcome-title h2{animation:poster-rise .7s cubic-bezier(.2,.72,.25,1) 3.12s both}
  .welcome-title__rule,.welcome-title__star{animation-duration:.7s;animation-timing-function:cubic-bezier(.2,.72,.25,1);animation-delay:3.12s;animation-fill-mode:both}
  .welcome-title__rule:first-child,.welcome-title__star:nth-child(2){animation-name:poster-rule-left}
  .welcome-title__star:nth-child(4),.welcome-title__rule:last-child{animation-name:poster-rule-right}
  .welcome-intro{animation:poster-rise .55s cubic-bezier(.2,.72,.25,1) 3.97s both}
  .welcome-gumby{animation:poster-rise .55s cubic-bezier(.2,.72,.25,1) 4.67s both}
  .photo-stack{animation:welcome-photo-composition .8s cubic-bezier(.2,.72,.25,1) 5.37s both}
  .welcome-closing{animation:poster-rise .5s cubic-bezier(.2,.72,.25,1) 6.24s both}
  .hero-after::after{content:"";position:absolute;z-index:3;right:clamp(18px,4vw,64px);bottom:0;left:clamp(18px,4vw,64px);height:1px;background:linear-gradient(to right,transparent 0%,var(--ochre) 18%,var(--ochre) 82%,transparent 100%);opacity:.62;pointer-events:none}
  @keyframes hero-welcome-divider{to{opacity:1}}
  @keyframes poster-rise{from{opacity:0;transform:translateY(24px)}to{opacity:1;transform:translateY(0)}}
  @keyframes welcome-photo-composition{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:translateY(0)}}
  @keyframes poster-rule-left{from{opacity:0;transform:translateX(-100vw)}to{opacity:1;transform:translateX(0)}}
  @keyframes poster-rule-right{from{opacity:0;transform:translateX(100vw)}to{opacity:1;transform:translateX(0)}}

  @media (max-width:900px){
    .hero-after__brand{height:auto}
    .hero-after__brand .logo-bar-stage{transform:none}
    .hero-after__brand .hero-actions{display:block;flex:none;margin-top:0;padding:18px 20px 8px}
    .hero-after__brand .home-specials__cta{margin:34px auto 0}
    .photo-stack{margin-left:calc(50% - 50vw);padding-block:34px 42px}
    .photo-track{--marquee-entry-offset:220px;animation-duration:132s}
    .photo{width:clamp(126px,34vw,154px)!important;height:clamp(116px,31vw,138px)}
    .photo--landscape{width:clamp(168px,45vw,198px)!important;height:clamp(108px,29vw,126px)}
    .photo--square{width:clamp(128px,35vw,150px)!important;height:clamp(128px,35vw,150px)}
    .photo--portrait{width:clamp(108px,29vw,126px)!important;height:clamp(150px,40vw,174px)}
    .photo img{object-fit:contain;background:#f6ecd8}
    .photo:nth-child(odd){transform:rotate(-2.4deg) translateY(8px)!important}.photo:nth-child(even){transform:rotate(2.2deg) translateY(-6px)!important}
    .photo:nth-child(3n){transform:rotate(-1.5deg) translateY(2px)!important}.photo:nth-child(4n){transform:rotate(2.7deg) translateY(-9px)!important}
  }

  /* Responsive poster composition: keep the branding stack bound to the boards,
     rather than inheriting desktop dimensions after the columns have collapsed. */
  @media (min-width:900px) and (max-width:1099px){
    .hero-after{padding-inline:clamp(20px,4vw,44px)}
    .hero-after__layout{
      grid-template-columns:minmax(180px,1fr) minmax(260px,1.2fr) minmax(180px,1fr);
      grid-template-areas:"lunch brand dinner";
      gap:clamp(16px,2.5vw,28px);
      max-width:none;
      align-items:center;
    }
    .daily-special{min-height:250px;padding:24px 18px}
    .hero-after__brand{height:250px;display:grid;grid-template-rows:128px 1fr auto;align-self:stretch}
    .hero-after__brand .logo-bar-stage{width:min(100%,310px);height:128px;transform:none}
    .hero-after__brand .hero-actions{display:flex;align-items:center;justify-content:center;margin:0;padding:0 12px}
    .hero-after__brand .hero-tag{margin:0}
    .hero-after__brand .home-specials__cta{align-self:center;justify-self:center;margin:0 0 2px}
    .hero-after__brand::before{inset:18px -10% -6px;filter:blur(7px)}
  }

  @media (min-width:600px) and (max-width:899px){
    .hero-after{padding:18px clamp(20px,5vw,42px) 26px}
    .hero-after__layout{
      grid-template-columns:repeat(2,minmax(0,1fr));
      grid-template-areas:"brand brand" "lunch dinner";
      gap:20px;
      max-width:680px;
      align-items:stretch;
    }
    .hero-after__brand{height:226px;display:grid;grid-template-rows:122px 1fr auto;align-self:stretch}
    .hero-after__brand .logo-bar-stage{width:min(100%,290px);height:122px;transform:none}
    .hero-after__brand .hero-actions{display:flex;align-items:center;justify-content:center;margin:0;padding:0 12px}
    .hero-after__brand .hero-tag{margin:0}
    .hero-after__brand .home-specials__cta{align-self:center;justify-self:center;margin:0 0 2px}
    .hero-after__brand::before{inset:16px -12% -6px;filter:blur(7px)}
    .daily-special{min-height:230px;padding:24px 18px}
  }

  @media (max-width:599px){
    .hero-after{padding:14px 16px 24px}
    .hero-after__layout{grid-template-columns:1fr;grid-template-areas:"brand" "lunch" "dinner";gap:16px;max-width:430px}
    .hero-after__brand{height:auto;display:grid;grid-template-rows:120px auto auto;gap:8px;align-self:auto}
    .hero-after__brand .logo-bar-stage{width:min(70vw,265px);height:120px;transform:none}
    .hero-after__brand .hero-actions{display:flex;align-items:center;justify-content:center;margin:0;padding:0 12px;min-height:34px}
    .hero-after__brand .hero-tag{margin:0}
    .hero-after__brand .home-specials__cta{align-self:center;justify-self:center;margin:0 0 2px}
    .hero-after__brand::before{inset:12px -14% -7px;filter:blur(6px)}
    .daily-special{min-height:200px;padding:22px 20px}
  }

  @media (max-width:620px){
    .photo-stack{width:calc(100% + 24px);margin:24px 0 0 -12px;padding:26px 0 30px;overflow:visible}
    .photo-track{display:flex;width:100%;transform:none!important;animation:none}
    .photo-sequence{display:flex;flex:1;flex-wrap:wrap;justify-content:center;gap:14px 8px;padding:0 6px}
    .photo-sequence[aria-hidden="true"]{display:none}
    .photo{width:clamp(96px,28vw,128px)!important;height:clamp(92px,26vw,118px);margin:0!important}
    .photo--landscape{width:clamp(122px,35vw,156px)!important;height:clamp(88px,25vw,108px)}
    .photo--square{width:clamp(98px,28vw,122px)!important;height:clamp(98px,28vw,122px)}
    .photo--portrait{width:clamp(82px,24vw,104px)!important;height:clamp(122px,35vw,152px)}
    .photo img{object-fit:cover;background:#f6ecd8}
    .welcome-closing{margin-top:18px}
  }
  @media (prefers-reduced-motion:reduce){
    .daily-special--lunch,.daily-special--dinner,.hero-after .home-specials__cta,.hero-after__brand::before,.welcome-title h2,.welcome-title__rule,.welcome-title__star,.welcome-intro,.welcome-gumby,.photo-stack,.welcome-closing{animation:none;opacity:1;transform:none}
    .photo-stack{overflow-x:auto;scroll-snap-type:x proximity;padding:34px 20px 42px}
    .photo-track{animation:none;width:max-content}
    .photo-sequence[aria-hidden="true"]{display:none}
    .photo{scroll-snap-align:center}
  }
</style>

<style is:global>
  /* Feed cards are created in the browser, so these selectors must not carry Astro's scope attribute. */
  #social{position:relative;isolation:isolate;padding-block:clamp(50px,7vw,86px);background-color:#d8d1c5;background-image:url('/images/facebook/facebook-wall.webp');background-size:1600px auto;background-repeat:repeat}
  #social::before,#social::after{content:"";position:absolute;z-index:0;right:0;left:0;height:6px;pointer-events:none}
  #social::before{top:0;background:linear-gradient(to bottom,rgba(67,42,23,.62) 0 1px,transparent 1px 4px,rgba(104,68,34,.3) 4px 5px,transparent 5px)}
  #social::after{bottom:0;background:linear-gradient(to top,rgba(67,42,23,.62) 0 1px,transparent 1px 4px,rgba(104,68,34,.3) 4px 5px,transparent 5px)}
  #social > *{position:relative;z-index:1}
  #social .social-heading{display:grid;grid-template-columns:minmax(24px,1fr) auto auto auto minmax(24px,1fr);align-items:center;gap:clamp(7px,1vw,12px);max-width:min(100%,760px);margin:0 auto clamp(14px,2vw,22px);color:var(--brick)}
  #social .social-heading :is(h1,h2){margin:0;color:currentColor;font-family:var(--display);font-size:clamp(1.55rem,2.65vw,2.5rem);line-height:1.04;text-align:center}
  #social .social-heading__star{font-size:clamp(.9rem,1.5vw,1.2rem);line-height:1}
  #social .social-heading__rule{height:2px;background:currentColor;opacity:.62}
  #menu-preview .menu-heading{display:grid;grid-template-columns:minmax(24px,1fr) auto auto auto minmax(24px,1fr);align-items:center;gap:clamp(7px,1vw,12px);max-width:min(100%,760px);margin:0 auto 30px;color:var(--cream)}
  #menu-preview .menu-heading :is(h1,h2){grid-column:3;margin:0;color:currentColor;font-family:var(--display);font-size:clamp(1.55rem,2.65vw,2.5rem);line-height:1.04;text-align:center}
  #menu-preview .menu-heading__star{font-size:clamp(.9rem,1.5vw,1.2rem);line-height:1}
  #menu-preview .menu-heading__rule{height:2px;background:currentColor;opacity:.62}
  #menu-preview .menu-heading .kicker,#menu-preview .menu-heading p{grid-column:1 / -1}
  #menu-preview .menu-heading .kicker{margin-top:2px}
  #menu-preview .menu-heading p{margin:0}
  /* The approved full-width artwork supplies one top, alternating two-plank middle, and one bottom. */
  #social .fb-billboard{width:min(calc(100% - 28px),1320px);margin:0 auto}
  #social .fb-billboard__top,#social .fb-billboard__bottom{width:100%;background-repeat:no-repeat;background-size:100% auto}
  #social .fb-billboard__top{position:relative;aspect-ratio:1600 / 221;background-image:url('/images/facebook/facebook-billboard-top.webp');background-position:center top}
  #social .fb-billboard__middle{position:relative;isolation:isolate;min-height:0;overflow:hidden;padding:clamp(130px,11vw,160px) clamp(68px,9vw,124px) clamp(18px,2vw,28px);box-sizing:border-box;background-color:#6d7978}
  #social .fb-billboard__middle-slices{position:absolute;z-index:0;inset:0;display:flex;flex-direction:column;overflow:hidden;pointer-events:none}
  #social .fb-billboard__middle-slice{flex:0 0 auto;width:100%;background:center top / 100% auto no-repeat}
  #social .fb-billboard__middle-slice--a{aspect-ratio:1600 / 194;background-image:url('/images/facebook/facebook-billboard-middle-a.webp')}
  #social .fb-billboard__middle-slice--b{aspect-ratio:1600 / 220;background-image:url('/images/facebook/facebook-billboard-middle-b.webp')}
  #social .fb-billboard__bottom{aspect-ratio:1600 / 261;background-image:url('/images/facebook/facebook-billboard-bottom.webp');background-position:center top}
  #social .fb-billboard:has(.fb-grid[data-count="1"]){max-width:520px}
  #social .fb-billboard:has(.fb-grid[data-count="2"]){max-width:820px}
  #social .fb-billboard:has(.fb-grid[data-count="3"]){max-width:1120px}
  #social .fb-board-label{position:absolute;z-index:2;top:calc(100% - clamp(14px,1.25vw,20px));right:12%;left:12%;color:#21170f;font-family:var(--display);font-size:clamp(.68rem,1.45vw,1.22rem);font-weight:700;letter-spacing:.08em;line-height:1.1;text-align:center;text-transform:uppercase;text-shadow:0 1px rgba(239,229,203,.2);transform:translateY(-50%)}
  #social .fb-grid{position:relative;z-index:1;display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:clamp(26px,3vw,44px) clamp(18px,2.4vw,32px);width:100%;min-height:0;align-items:start}
  #social .fb-grid[data-count="1"]{grid-template-columns:1fr}
  #social .fb-grid[data-count="2"]{grid-template-columns:repeat(2,minmax(0,1fr))}
  #social .fb-grid[data-count="3"]{grid-template-columns:repeat(3,minmax(0,1fr))}
  /* The generated sheet supplies the paper silhouette. Drop shadows follow its alpha instead of drawing a rectangular card behind it. */
  /* Every item has the same card, placard, and button rows so poster tops remain aligned. */
  #social .fb-item{--post-tilt:-1deg;--post-hover-tilt:-.4deg;--poster-sheet:url('/images/facebook/fb-poster-1.webp');display:grid;grid-template-rows:auto auto auto;justify-items:center;align-items:start;min-width:0}
  #social .fb-card{--poster-inset:clamp(18px,1.65vw,22px);position:relative;z-index:0;grid-row:2;display:flex;flex-direction:column;width:100%;min-width:0;aspect-ratio:2 / 3;overflow:visible;padding:var(--poster-inset);background:var(--poster-sheet) center / 100% 100% no-repeat;border:0;box-shadow:none;filter:drop-shadow(2px 4px 3px rgba(65,40,22,.22)) drop-shadow(8px 15px 22px rgba(43,27,20,.24));transform:rotate(var(--post-tilt));transform-origin:center top;transition:transform .2s ease,filter .2s ease}
  #social .fb-card::before{display:none}
  #social .fb-card::after{content:"";position:absolute;z-index:2;top:-11px;left:50%;width:16px;height:16px;border:1px solid #4a351e;border-radius:50%;background:radial-gradient(circle at 31% 25%,#d7b465 0 11%,#a47b37 31%,#765326 58%,#48321e 100%);box-shadow:inset -2px -2px 2px rgba(52,31,14,.4),inset 1px 1px 1px rgba(238,210,133,.52),2px 4px 4px rgba(43,27,20,.48);transform:translateX(-50%);pointer-events:none}
  #social .fb-item:nth-child(4n + 2){--poster-sheet:url('/images/facebook/fb-poster-2.webp');--post-tilt:.6deg;--post-hover-tilt:.25deg}
  #social .fb-item:nth-child(4n + 3){--poster-sheet:url('/images/facebook/fb-poster-3.webp');--post-tilt:-.4deg;--post-hover-tilt:-.15deg}
  #social .fb-item:nth-child(4n + 4){--poster-sheet:url('/images/facebook/fb-poster-4.webp');--post-tilt:.8deg;--post-hover-tilt:.35deg}
  #social .fb-item:nth-child(4n + 2) .fb-card,#social .fb-item:nth-child(4n + 3) .fb-card,#social .fb-item:nth-child(4n + 4) .fb-card{transform:rotate(var(--post-tilt))}
  #social .fb-card:hover{transform:translateY(-3px) rotate(var(--post-hover-tilt));filter:drop-shadow(2px 5px 3px rgba(65,40,22,.22)) drop-shadow(10px 20px 27px rgba(43,27,20,.27))}
  #social .fb-item:nth-child(2) .fb-card:hover,#social .fb-item:nth-child(3) .fb-card:hover,#social .fb-item:nth-child(4) .fb-card:hover{transform:translateY(-3px) rotate(var(--post-hover-tilt))}
  /* The caption art is a fixed placard; its live text is clipped within the printed center field. */
  #social .fb-caption{grid-row:1;display:grid;place-items:center;width:min(calc(100% - 12px),220px);aspect-ratio:420 / 280;margin:clamp(24px,2.2vw,30px) 0 12px;padding:clamp(26px,3.2vw,34px) clamp(32px,4vw,42px);box-sizing:border-box;background:url('/images/facebook/facebook-caption.webp') center / 100% 100% no-repeat;color:#392315;font-family:var(--body);font-size:clamp(.82rem,1.05vw,.94rem);line-height:1.28;text-align:center;text-shadow:0 1px rgba(255,243,214,.45);white-space:pre-line}
  #social .fb-caption p{display:-webkit-box;width:100%;margin:0;overflow:hidden;-webkit-box-orient:vertical;-webkit-line-clamp:2;line-clamp:2}
  /* The printed artwork and metadata stay parallel with the one rotating poster sheet. */
  #social .fb-media,#social .fb-media img,#social .fb-footer{transform:none}
  #social .fb-media{position:relative;z-index:0;display:grid;flex:1 1 auto;min-height:0;width:100%;margin-top:8px;padding:0;border:0;background:transparent;box-shadow:none;cursor:zoom-in}
  #social .fb-media:focus-visible{outline:3px solid var(--gold);outline-offset:4px}
  #social .fb-media img{display:block;width:100%;height:100%;min-height:0;object-fit:contain;filter:sepia(.05) saturate(.96) contrast(.99);background:transparent}
  #social .fb-text-media{display:-webkit-box;flex:1 1 auto;min-height:0;margin:0;padding:clamp(12px,2vw,20px);overflow:hidden;color:var(--ink);font-family:var(--body);font-size:clamp(.95rem,1.35vw,1.08rem);line-height:1.45;text-align:center;white-space:pre-line;-webkit-box-orient:vertical;-webkit-line-clamp:9;line-clamp:9}
  #social .fb-text-media--short{display:flex;align-items:center;justify-content:center;font-family:var(--display);font-size:clamp(1.1rem,1.6vw,1.45rem);line-height:1.25}
  #social .fb-footer{display:flex;align-items:center;justify-content:center;min-height:30px;padding:7px 3px 0;color:var(--ink);font-family:var(--display);font-size:clamp(.64rem,.9vw,.76rem);font-weight:700;letter-spacing:.1em;text-align:center;text-transform:uppercase}
  #social .fb-date{color:var(--ink)}
  #social .fb-post-link{grid-row:3;display:inline-flex;align-items:center;justify-content:center;min-height:30px;margin-top:clamp(26px,2.2vw,32px);padding:6px 13px;border:1px solid rgba(177,137,72,.72);background:#25170f;box-shadow:inset 0 1px rgba(255,240,205,.16),inset 0 -1px rgba(0,0,0,.3),0 2px 0 rgba(43,27,20,.44);color:var(--cream);font-family:var(--label);font-size:.72rem;font-weight:700;letter-spacing:.12em;line-height:1;text-decoration:none;text-transform:uppercase;white-space:nowrap}
  #social .fb-post-link:hover,#social .fb-post-link:focus-visible{background:#3a2518;color:var(--cream);box-shadow:inset 0 1px rgba(255,240,205,.2),inset 0 -1px rgba(0,0,0,.32),0 2px 0 rgba(43,27,20,.44)}
  #social .fb-unavailable{grid-column:1 / -1;justify-self:center;width:min(100%,34rem);margin:6px auto 0;padding:30px;border:2px solid rgba(83,51,26,.55);background:#f0dfbd;box-shadow:3px 4px 0 rgba(43,27,20,.18);color:var(--text-muted);text-align:center;font-size:1.05rem}
  #social .fb-unavailable-arrow{display:block;margin-top:8px;color:var(--brick);font-family:var(--display);font-size:2rem;line-height:1}
  #social .social-cta{margin-top:clamp(30px,4vw,46px);text-align:center}
  #social .social-cta .btn{position:relative;border:1px solid rgba(238,224,196,.56);background:var(--brick);box-shadow:inset 0 1px rgba(255,255,255,.2),0 3px 0 rgba(67,30,24,.35);color:var(--cream)}
  #social .social-cta .btn::before,#social .social-cta .btn::after{content:'★';margin:0 10px;color:var(--gold);font-size:.75em}
  #social .social-cta .btn:hover,#social .social-cta .btn:focus-visible{background:var(--brick-hover);transform:translateY(-2px);box-shadow:inset 0 1px rgba(255,255,255,.2),0 5px 0 rgba(67,30,24,.35)}
  @media (max-width:1100px){#social .fb-billboard{max-width:820px}#social .fb-billboard__middle{padding:clamp(118px,18vw,150px) clamp(54px,9vw,76px) clamp(18px,3vw,28px)}#social .fb-board-label{right:10%;left:10%;font-size:clamp(.68rem,1.65vw,1.04rem)}#social .fb-grid,#social .fb-grid[data-count="3"],#social .fb-grid[data-count="4"]{grid-template-columns:repeat(2,minmax(0,1fr))}}
  @media (max-width:600px){#social .social-heading,#menu-preview .menu-heading{grid-template-columns:minmax(16px,1fr) auto auto auto minmax(16px,1fr);gap:6px}#social .social-heading{margin-bottom:14px}#social .social-heading :is(h1,h2),#menu-preview .menu-heading :is(h1,h2){font-size:clamp(1.4rem,7.1vw,1.8rem)}#social .fb-billboard{width:min(calc(100% - 20px),440px);max-width:440px}#social .fb-billboard__middle{padding:clamp(58px,15vw,74px) clamp(32px,10vw,44px) clamp(18px,4vw,28px)}#social .fb-board-label{right:7%;left:7%;font-size:clamp(.68rem,3.1vw,.84rem);letter-spacing:.06em}#social .fb-grid,#social .fb-grid[data-count="2"],#social .fb-grid[data-count="3"],#social .fb-grid[data-count="4"]{grid-template-columns:1fr;gap:30px}#social .fb-item{--post-tilt:-.7deg;--post-hover-tilt:-.3deg}#social .fb-item:nth-child(even){--post-tilt:.7deg;--post-hover-tilt:.3deg}#social .fb-item .fb-card,#social .fb-item:nth-child(n) .fb-card{transform:rotate(var(--post-tilt))}#social .fb-card::after{top:-10px;width:15px;height:15px}#social .fb-card:hover,#social .fb-item:nth-child(n) .fb-card:hover{transform:translateY(-2px) rotate(var(--post-hover-tilt))}#social .fb-caption{width:min(calc(100% - 12px),220px);font-size:clamp(.82rem,3.6vw,.94rem)}}
  @media (prefers-reduced-motion:reduce){#social .fb-card{transition:none}#social .fb-card:hover,#social .fb-item:nth-child(n) .fb-card:hover{transform:rotate(var(--post-tilt))}}
</style>

<script>
  document.addEventListener('error', (event) => {
    const image = event.target;
    if (image instanceof HTMLImageElement && image.matches('.photo img')) {
      image.closest<HTMLElement>('.photo')?.remove();
    }
  }, true);

  document.addEventListener('image-lightbox:open', (event) => {
    const trigger = (event as CustomEvent<{ trigger?: HTMLElement }>).detail?.trigger;
    trigger?.closest<HTMLElement>('.photo-stack')?.setAttribute('data-lightbox-paused', 'true');
  });
  document.addEventListener('image-lightbox:close', (event) => {
    const trigger = (event as CustomEvent<{ trigger?: HTMLElement }>).detail?.trigger;
    trigger?.closest<HTMLElement>('.photo-stack')?.removeAttribute('data-lightbox-paused');
  });

  const social = document.getElementById('social');
  const feed = document.getElementById('fbFeed');
  const billboardMiddle = document.querySelector<HTMLElement>('.fb-billboard__middle');
  const billboardMiddleSlices = document.querySelector<HTMLElement>('.fb-billboard__middle-slices');
  let billboardSliceSignature = '';

  function syncBillboardMiddleSlices() {
    if (!billboardMiddle || !billboardMiddleSlices) return;

    const width = billboardMiddle.clientWidth;
    const height = billboardMiddle.clientHeight;
    if (!width || !height) return;

    const sliceCycleHeight = width * ((194 + 220) / 1600);
    const sliceCount = Math.ceil(height / sliceCycleHeight) * 2 + 2;
    const signature = `${Math.round(width)}:${Math.round(height)}:${sliceCount}`;
    if (signature === billboardSliceSignature) return;

    const slices = document.createDocumentFragment();
    for (let index = 0; index < sliceCount; index += 1) {
      const slice = document.createElement('div');
      slice.className = `fb-billboard__middle-slice fb-billboard__middle-slice--${index % 2 === 0 ? 'a' : 'b'}`;
      slices.append(slice);
    }

    billboardMiddleSlices.replaceChildren(slices);
    billboardSliceSignature = signature;
  }

  if (billboardMiddle && billboardMiddleSlices) {
    const billboardSliceObserver = new ResizeObserver(syncBillboardMiddleSlices);
    billboardSliceObserver.observe(billboardMiddle);
    syncBillboardMiddleSlices();
  }

  function showFacebookFallback() {
    if (!feed || !social) return;

    feed.replaceChildren();

    const message = document.createElement('p');
    message.className = 'fb-unavailable';
    message.textContent = 'Follow us for the latest updates.';

    const arrow = document.createElement('span');
    arrow.className = 'fb-unavailable-arrow';
    arrow.setAttribute('aria-hidden', 'true');
    arrow.textContent = '↓';

    message.append(arrow);
    feed.append(message);

    delete feed.dataset.count;
    social.hidden = false;
  }

  function loadFacebookFeed() {
    fetch('/api/facebook-feed', {
      headers: { Accept: 'application/json' },
    })
    .then((response) => (response.ok ? response.json() : null))
    .then((payload) => {
      if (
        !feed ||
        !social ||
        !Array.isArray(payload?.posts) ||
        payload.posts.length === 0
      ) {
        showFacebookFallback();
        return;
      }

      feed.replaceChildren();

      for (const post of payload.posts.slice(0, 4)) {
        if (!post?.permalinkUrl) continue;

        const item = document.createElement('div');
        item.className = 'fb-item';

        const card = document.createElement('article');
        card.className = 'fb-card';

        if (post.imageUrl) {
          const media = document.createElement('button');
          media.type = 'button';
          media.className = 'fb-media';
          media.dataset.lightboxSrc = post.imageUrl;
          media.dataset.lightboxAlt = 'Facebook post image';
          if (post.message) media.dataset.lightboxCaption = post.message;
          media.dataset.lightboxActionUrl = post.permalinkUrl;
          media.dataset.lightboxActionLabel = 'View on Facebook';
          media.dataset.lightboxActionNewTab = 'true';
          media.setAttribute('aria-label', 'View Facebook post image larger');
          const image = document.createElement('img');
          image.src = post.imageUrl;
          image.alt = '';
          image.loading = 'lazy';
          media.append(image);
          card.append(media);
        } else {
          const textNotice = document.createElement('p');
          textNotice.className = 'fb-text-media';
          if (post.message && post.message.trim().length <= 84) textNotice.classList.add('fb-text-media--short');
          textNotice.textContent = post.message || "Grayz'n Buffalo";
          card.append(textNotice);
        }

        const footer = document.createElement('div');
        footer.className = 'fb-footer';

        if (post.postedLabel) {
          const date = document.createElement('span');
          date.className = 'fb-date';
          date.textContent = post.postedLabel;
          footer.append(date);
        }

        card.append(footer);

        const caption = document.createElement('div');
        caption.className = 'fb-caption';
        const captionText = document.createElement('p');
        captionText.textContent = post.message?.trim() || 'See Facebook for full post details.';
        caption.append(captionText);
        item.append(caption, card);

        const postLink = document.createElement('a');
        postLink.className = 'fb-post-link';
        postLink.href = post.permalinkUrl;
        postLink.target = '_blank';
        postLink.rel = 'noopener';
        postLink.textContent = 'View Post';

        item.append(postLink);
        feed.append(item);
      }

      if (feed.childElementCount > 0) {
        feed.dataset.count = String(feed.childElementCount);
        social.hidden = false;
      } else {
        showFacebookFallback();
      }
    })
      .catch(showFacebookFallback);
  }

  function scheduleFacebookFeed() {
    const requestIdleCallback = (globalThis as typeof globalThis & {
      requestIdleCallback?: (callback: () => void, options?: { timeout: number }) => number;
    }).requestIdleCallback;

    if (requestIdleCallback) {
      requestIdleCallback(loadFacebookFeed, { timeout: 4000 });
    } else {
      setTimeout(loadFacebookFeed, 1500);
    }
  }

  if (document.readyState === 'complete') {
    scheduleFacebookFeed();
  } else {
    window.addEventListener('load', scheduleFacebookFeed, { once: true });
  }
</script>
